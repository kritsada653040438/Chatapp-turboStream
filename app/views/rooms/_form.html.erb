<div id='actions'>
  <%= form_with(model: room) do |form| %>
    <div>
      <%= form.label :name, style: "display: block" %>
      <%= form.text_field :name %>
    </div>

    <div id="room-form-submit-btn">
      <%= form.submit %>
    </div>
  <% end %>
</div>
